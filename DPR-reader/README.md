# DPR reader model files

The hierarchical retrieval preprocessing tool expects a local Hugging Face DPR reader directory here. Download the approved `facebook/dpr-reader-single-nq-base` files and place them in this directory.

Model weights are intentionally excluded from the repository.
