# Optional GloVe files

Place the converted GloVe files used by the embedding-based metrics in this directory. Large embedding files are intentionally excluded from the repository.

The code accepts an alternate location through the `MKESC_GLOVE_DIR` environment variable.
