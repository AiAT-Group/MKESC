# Optional METEOR resource

The METEOR 1.5 wrapper can use the upstream English paraphrase table named `paraphrase-en.gz`. That large external resource is intentionally not included in this repository.

Place the file in this directory before running inference with `ADD_NLG_EVAL=true` if the selected METEOR configuration requires it.
